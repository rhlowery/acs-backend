apiVersion: v1
kind: Service
metadata:
  name: {{ include "acs-backend.fullname" . }}
  labels:
    {{- include "acs-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acs-backend.selectorLabels" . | nindent 4 }}
